% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotly_mcplotly.R
\name{plotly_mcplotly}
\alias{plotly_mcplotly}
\title{Create a Plotly scatter plot with flexible aesthetics}
\usage{
plotly_mcplotly(
  df,
  x_col = "V1",
  y_col = "V2",
  text_col = "umap_text",
  colour_col = NULL,
  single_colour = "purple",
  shape_col = NULL,
  size_col = NULL,
  opacity = 1,
  size_range = c(5, 30),
  size = 1
)
}
\arguments{
\item{df}{Data frame containing the data to plot.}

\item{x_col}{Character. Name of column to use for x-axis. Default is "V1".}

\item{y_col}{Character. Name of column to use for y-axis. Default is "V2".}

\item{text_col}{Character. Name of column to use for hover text. Default is "umap_text".}

\item{colour_col}{Character. Optional name of column to colour points by. Can be
categorical (will be converted to factor) or continuous (numeric). If NULL,
uses \code{single_colour} instead.}

\item{single_colour}{Character. Colour to use when \code{colour_col} is NULL.
Default is "purple".}

\item{shape_col}{Character. Optional name of column to use for point sizes.}

\item{opacity}{Numeric. Point opacity between 0 and 1. Default is 1.}

\item{size_range}{Numeric. Vector to represent the max and min scaling for size when mapped to column.}

\item{size}{Numeric. Point size. Default is 1.}
}
\value{
A plotly object.
}
\description{
A wrapper around \code{plotly::plot_ly()} for quickly creating scatter plots
with optional colour and shape mappings. Particularly useful for visualising
dimensionality reduction outputs (e.g., UMAP, t-SNE) with hover text.
}
\examples{
\dontrun{
# Simple plot with single colour
plotly_mcplotly(umap_data, x_col = "umap_1", y_col = "umap_2")

# Colour by categorical variable
plotly_mcplotly(umap_data, colour_col = "cluster")

# Colour by continuous variable
plotly_mcplotly(umap_data, colour_col = "sentiment_score")

# Colour and shape by different variables
plotly_mcplotly(umap_data, colour_col = "topic", shape_col = "source")
}

}
